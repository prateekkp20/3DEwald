using namespace std;  // this makes all the functions, classes, or a variable in std to become visible so instead of using std::cout you can use std directly
#include <iostream>  // input output functions can be used
#include <math.h>    // for using the math mode 
#include <iomanip>  // used for formatting the output with setw, scientific output etc. 
#include <fstream>  //used for file read, write and append
#include <stdlib.h> //This header defines several general purpose functions, including dynamic memory management, random number generation, communication with the environment, integer arithmetics, searching, sorting and converting.
#include <assert.h>            //assert.h defines one macro function that can be used as a standard debugging tool. For example it asserts whether a function has an error using stderror and then calls abort()  
#include <time.h> //This header file contains definitions of functions to get and manipulate date and time information.
#include <sys/timeb.h>
#include <cmath>
#include <string>
#include <sstream>  //string streaming, used for manipulating strings!!!
#include <Eigen/Eigenvalues>
using namespace Eigen;
#include <stdio.h>
#include<string.h>


